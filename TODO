TODO

Sicherheit
controller anpassen, dass sie nur die daten des nutzers weitergeben und nicht alle, außer beim admin
    - If user.is_admin? dann .all | else (Nur daten vom user) end
    - companies, registered_users, restrictions, depots -> admin
views anpassen, dass sie bei falscher rolle ZUGANG VERWEIGERT anzeigen
    - wie bei company index.html.erb
    - orders_controller, vehicles_controller, drivers_controller, costumers_controller

Funktion
dropdowns nur eigene Drivers, vehicles etc.
.csv inport für testdaten -> über rails c ausführen -> lädt alles aus dem angegeben Ordner tmp/csv ->
Google API blockt einen wegen zu vielen Anfragen -> Wird der API Schlüssel genutzt???
https://github.com/joshcrews/google-directions-ruby
https://console.developers.google.com/project/eternal-skyline-106615/apiui/credential/key/0

Form
Sprache in show and _form anpassen
Alle Objekte (Driver, Order etc.) automatisch mit der Company des users associeren
    - tours_controller
f.association :user in allen _form ergänzen um , :include_blank => false, :label_method => :form_label %>
Alle Einträge eines Planer seiner Firma zuteilen und gleiches für Driver
    - Create aller controller die company_id setzen
    - Update auch um sicher zugehen