TODO

löschen von Fahrer löscht auch Tour und Elmente!

alle controller so anpassen, dass nur eigene daten gesehen werden als Planer
und das Fahrer bei Tour nur seine sieht

Alle Einträge eines Planer seiner Firma zuteilen und gleiches für Driver
  
handy-gps für position von vehicle 
    + vehicle.position regelmäßig updaten
        
UTC zu MESZ ändern, damit Zeiteingabe stimmt        
        
tours_controller new eingabe für generate überarbeiten (Input params filtern)

controller anpassen, dass sie nur die daten des nutzers weitergeben und nicht alle, außer beim admin
    - If user.is_admin? dann .all | else (Nur daten vom user) end

index.html.erb überarbeiten für neue Attribute (name etc)

Alle Objekte (Driver, Order etc.) automatisch mit der Company des users associeren
    - tours_controller
    
touren bilden -> fahrzeit errechnen? - https://github.com/alexreisner/geocoder
    -   nominatimaktiv
    
OrderTour-Objekte erzeugen und speichern

Allgemein die Änderungen in DB Sppeichern Objekt.save und .create

ansicht für role faher - https://github.com/apneadiving/Google-Maps-for-Rails
    - google maps ist eingebunden, orte/tour noch reinschmeißen - genrating jason
    - Driver tour abarbeiten (OrderTour-Elemente bestätigen und Tour abschließen)
        - start, pause, done, end
        - Tour vom Driver lösen, wenn fertig


.csv inport für testdaten

Auf sensiblen Seiten Rolle nochmal überprüfen (ADmin-Seiten etc.)

signin entferen
-  new user (password?)

_form - edit, new, delete von user funktioniernen nicht

user name = username für simple_form (sonst Objekt-Bezeichnung)

!!! order -> Adresse eingeben, bzw. übernehmen von Kunden, auch anzeigen im view

Sprache in show and _form anpassen

SLEEP-param bei Geocoder, um die Abfrage pro Sekunde nicht zu überschreiten

DONE
Geocoder eingefügt