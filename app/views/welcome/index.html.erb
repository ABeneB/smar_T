<% if current_user %>



    <% if current_user.role == "admin" %>
        <h4><%= t('welcome_admin', name: current_user.username) %></h4>
    <% elsif current_user.role =="planer" %>
        <p>Jedes vom Planer angelegte Objekt wird automatisch mit ihm und dessen Company verbunden</p>
        <p>Im folgenden ist eine Übersicht über die Position jedes Fahrers zu sehen.</p>
        <p>Die jeweilige Tour kann unter Touren eingesehen werden.</p>
        <p>Eine Angabe über die einzelnen Ziele jeder Tour sind zusätzlich bei jedem Fahrer einzusehen.</p>
        <div style='width: 800px;'>
            <div id="map" style='width: 900px; height: 400px;'></div>
        </div>

        
        <script>
        handler = Gmaps.build('Google');
        handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
          markers = handler.addMarkers(<%=raw @hash.to_json %>);
          handler.bounds.extendWith(markers);
          handler.fitMapToBounds();
        });
        </script>
        
    <% elsif current_user.role =="driver" %> 
        <p>Unter Tour können Sie die Ihnen zugeteilte Tour einsehen.</p>
    <% else%> 
        <p>Willkommen, Sie haben noch keien Rolle. Wenden sie sich an den Admin.</p>
    <% end %>

<% else %>
    <p>Willkommen, Bitte einloggen.</p>
<% end %>

