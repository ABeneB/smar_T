<% if current_user %>
    <% if current_user.role == "admin" %>
        <h4>Entwickler Optionen</h4>
        <div class="alert alert-info fade in">
          <p>Informationen zur Funktion "Zurücksetzen"</p>
            <ul>
              <li>Admin ein Unternehmen zuweisen</li>
              <li>Löschen aller Touren (Tours) und Aufträge (Orders)</li>
              <li>Import der Testdaten (tmp/csv/*.csv)</li>
              <li>Neuerstellen der Fahrer und Fahrzeuge</li>
              <li>Erstellen der Touren</li>
            </ul>
        </div>
        <%= button_to 'Zurücksetzen', developer_reset_database_path, method: :post %>
    <% end %>
<% end %>
