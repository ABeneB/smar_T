<%- model_class = Company -%>
<% if current_user && ( current_user.role == "admin" || (current_user.role == "superadmin" && current_user.company_id?)) %>
  <% if current_user.company_id == @company.id%>
    <div class="page-header">
      <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => Company %></h1>
    </div>
    <%= render :partial => 'form' %>
  <% else %>
      <div class="alert alert-danger">Falsches Unternehmen!</div>
      ZUGANG VERWEIGERT
  <% end %>
<% else %>
ZUGANG VERWEIGERT
<% end %>
