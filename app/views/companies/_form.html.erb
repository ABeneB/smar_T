<% if current_user.role == "admin" %>
  <%= simple_form_for @company do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :telefon %>
    <%= f.association :user, :include_blank => false, :label_method => :form_label %>
    <%= f.button :submit %>
  <% end %>
<% end %>

