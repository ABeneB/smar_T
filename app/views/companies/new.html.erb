<%- model_class = Company -%>
<% if current_user && current_user.role == "superadmin" && !current_user.company_id?%>
    <div class="page-header">
      <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => Company %></h1>
    </div>
    <%= render :partial => 'form' %>

<% else %>
ZUGANG VERWEIGERT
<% end %>