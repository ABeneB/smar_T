<% if current_user.role == "admin" %>
  <%= simple_form_for @user, :url => registered_users_path, :method => :post do |f| %>
    <%= f.input :username %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :role %>
    <%= f.button :submit %>
  <% end %>
<% end %>