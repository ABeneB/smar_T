<%- model_class = Vehicle -%>
<% if current_user %>
    <% if current_user.is_admin? || current_user.is_planer? || (current_user.is_superadmin? && current_user.company_id?)%>
        <div class="page-header">
          <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => Vehicle %></h1>
        </div>
        <%= render :partial => 'form' %>
    <% else %>
    ZUGANG VERWEIGERT
    <% end %>
<% else %>
ZUGANG VERWEIGERT
<% end %>