<%- model_class = Depot -%>
<% if current_user %>
    <% if current_user.is_admin? || (current_user.is_superadmin? && current_user.company_id?)%>
      <% if current_user.company_id == @depot.company_id%>
        <div class="page-header">
          <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => Depot %></h1>
        </div>
        <%= render :partial => 'form' %>
      <% else %>
          <div class="alert alert-danger">Falsches Unternehmen!</div>
          ZUGANG VERWEIGERT
      <% end %>
    <% else %>
    ZUGANG VERWEIGERT
    <% end %>
<% else %>
ZUGANG VERWEIGERT
<% end %>
