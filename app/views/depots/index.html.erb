<%- model_class = Depot -%>
<div class="page-header">
  <h1><%=t '.title', :default => Depot %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Unternehmen</th>
      <th>Name</th>
      <th>Adresse</th>
      <th>Einladedauer</th>
      <th>Erstellt am</th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @depots.each do |depot| %>
      <tr>
        <td><%= depot.id %></td>
        <td><%= depot.company.name %></td>
        <td><%= depot.name %></td>
        <td><%= depot.address %></td>
        <td><%= depot.duration %></td>
        <td><%=l depot.created_at, format: :long %></td>
        <td><%= link_to 'Show', depot, :class => 'btn btn-info btn-xs' %></td>
        <td><%= link_to 'Edit', edit_depot_path(depot), :class => 'btn btn-warning btn-xs' %></td>
        <td><%= link_to 'Destroy', depot, method: :delete, 
        data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger btn-xs' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_depot_path,
            :class => 'btn btn-primary' %>
