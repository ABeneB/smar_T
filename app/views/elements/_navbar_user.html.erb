<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "smar[T]", defined?(root_path) ? root_path : "#", class: "navbar-brand" %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="navbar-collapse-1">
    <ul class="nav navbar-nav">
      <% if defined?(Devise) %>
        <% if user_signed_in? %>
            <!-- Kein Zugirff, Wartet auf Rolle -->
        <% else %>
          <!-- Sollte nicht der Fall sein -->
        <% end %>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
     <li><%= link_to (fa_icon "info-circle 1x"), about_index_path, :title => t('about.index.title')%></li>
      <% if defined?(Devise) %>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= image_tag('Account.svg', class: 'account-icon') %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            </ul>
        <% else %>
          <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
      <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>