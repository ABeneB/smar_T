<%- model_class = Driver -%>
<% if current_user %>
    <% if current_user.is_admin? || current_user.is_planer?%>
        <div class="page-header">
          <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => Driver %></h1>
        </div>
        <%= render :partial => 'form' %>
    <% else %>
    ZUGANG VERWEIGERT
    <% end %>
<% else %>
ZUGANG VERWEIGERT
<% end %>
