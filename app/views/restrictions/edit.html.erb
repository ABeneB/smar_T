<%- model_class = Restriction -%>
<% if current_user &&  current_user.is_admin? %>
    <div class="page-header">
      <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => Restriction %></h1>
    </div>
    <%= render :partial => 'form' %>
<% else %>
ZUGANG VERWEIGERT
<% end %>