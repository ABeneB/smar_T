<%- model_class = Restriction -%>
<% if current_user && if current_user.is_admin? %>
    <div class="page-header">
      <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => Restriction %></h1>
    </div>
    <%= render :partial => 'form' %>

<% else %>
ZUGANG VERWEIGERT
<% end %>